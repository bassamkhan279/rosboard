#!/usr/bin/env python3
import threading
import subprocess
import pathlib
import rosboard.login_server

print("[ROSBoard] 🚀 Starting combined backend + login server...")

BASE_DIR = pathlib.Path(__file__).parent / "rosboard"

def start_backend():
    print("[ROSBoard] 🔧 Launching backend on port 8899...")
    backend_file = BASE_DIR / "main.py"
    if not backend_file.exists():
        print(f"[ERROR] Backend file not found at: {backend_file}")
        return
    subprocess.Popen(["python3", str(backend_file), "--port", "8899"])

# Start backend in background thread
backend_thread = threading.Thread(target=start_backend, daemon=True)
backend_thread.start()

# Start login server (main thread)
rosboard.login_server.main()

print("[ROSBoard] ✅ All services running. Access:")
print("   Login:   http://localhost:8888")
print("   Backend: http://localhost:8899")
